import ReactDOM from "react-dom/client";

import App from "./App.jsx";
import "./index.css"; // vite injects the style dynamically

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
